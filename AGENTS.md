# ClipHistory 项目工作指引

本文件是 Codex 及其他开发协作者进入项目后的首要说明文件，作用类似 `CLAUDE.md`。开始工作前必须先阅读本文件及下列标准文档。

## 必读文件

1. 产品需求：`docs/01-product-requirements.md`
2. 技术架构：`docs/02-technical-architecture.md`
3. UI/UX 设计规范：`docs/03-ui-ux-guidelines.md`
4. 分阶段开发计划：`docs/04-development-plan.md`
5. 质量、安全与测试规范：`docs/05-quality-security-testing.md`
6. 文档与日志规则：`docs/06-documentation-and-logging.md`
7. 开发日志说明：`dev-logs/README.md`

## 工作原则

- 小步推进：一次只处理当前阶段中一个可验证的小目标。
- 先计划后修改：开始编码前说明本次目标、影响范围和验收方式。
- 稳定优先：核心剪贴板功能先于动画、复杂主题等非必要功能。
- 隐私优先：历史数据默认仅保存在本机，不上传、不联网。
- 保留用户改动：不得覆盖或撤销与当前任务无关的修改。
- 每次修改后执行与风险相称的检查或测试，不允许以“应该可以”代替验证。
- 发现需求冲突、数据丢失风险或权限扩大时，停止相关修改并向用户确认。

## 每次开发会话流程

1. 阅读本文件和与任务相关的 `docs/` 文档。
2. 运行 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Update-DevLog.ps1 -Start`，创建或补全当天日志；该命令只对当前进程生效，不修改系统执行策略。
3. 在当天日志的“今日计划”中写入本次小目标。
4. 只实现该目标所需的最小变更。
5. 执行测试或人工验收，并记录结果。
6. 运行日志脚本记录已完成事项和下一步待办。
7. 如果需求、架构或规范发生变化，同步更新对应 `docs/` 文件。

## 范围控制

- 未经用户确认，不擅自增加云同步、账号、OCR、遥测或联网功能。
- 未经用户确认，不改变已确定的保存期限（1、3、5 天）和数据处理规则。
- 不在第一版中保存被复制文件的本体，只保存文件路径。
- 任何可能删除用户数据的功能都必须有测试和清晰的用户提示。
